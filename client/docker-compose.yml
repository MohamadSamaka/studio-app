version: '3.8'

services:
  web:
    build: .
    container_name: my-expo-web-app
    ports:
      - "8080:80"  # Map host port 8080 to container port 80
    volumes:
      - ./web-build:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped